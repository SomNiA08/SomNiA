# CLAUDE.md — Claude Code 배선 (환경 · 권한 · 프리미티브)

> **먼저 `SOUL.md`(헌법)와 `AGENTS.md`(공통 규칙)를 읽고 그걸 우선해 따르라.**
> 서열: 사용자 > SOUL.md > AGENTS.md > **CLAUDE.md**. 이 파일은 *어떻게 배선됐나*만 다룬다(왜·무엇은 SOUL/AGENTS에).

## 프리미티브 지도 (커맨드 → 스킬 ↔ 훅 · 7요소)

| 종류 | 누가 트리거 | 무엇 |
|---|---|---|
| **커맨드** | 사람/CronJob | `/meeting <안건>`(회의 시작) · `/meeting-round`(라운드 1회) · `/retro`(회고) |
| **스킬·검사** | 모델·커맨드 | `checks/step-diff.mjs`(걸음 산출 경계 게이트 · W5) — 재사용 절차 (스킬 자체는 현재 없음; 안정화 후 패키징) |
| **훅** | Claude Code(자동) | `session-start`(재주입+TOP-WALLS) · `pre-tool-use`(engine 기반 덮어쓰기 차단) · `post-tool-use`(slop 경고) · `stop-retro-guard`(회고 미완 차단) — 차단·경고·포기는 `ledger`(사건 장부)에 자동 기록 |
| **에이전트** | 커맨드가 배분 | `moderator`·`panelist-*`·`devils-advocate`·`scribe`·`retrospector` |
| **MCP** | 에이전트(규약 하에) | 기본 없음 — 필요해질 때 §1 규약에 따라 등록 표에 올린 뒤 사용 |
| **환경/권한** | 설정 | Windows · Node 훅 · 에이전트별 tools 제한 (아래 §2·§3) |
| **CronJob** | 스케줄러 | 정기 회의·정기 회고 예약 (아래 §6) |

> 컨텍스트 유지 규약: 세션 시작·재개·압축 때 `session-start` 훅이 SOUL 핵심 + **TOP-WALLS(상위 벽 요약)** +
> 안건 + 상태를 **자동 재주입**한다. 이것이 컨텍스트 유지의 유일한 공식 통로다. (플러그인화: 위 요소가 안정되면
> command+skill+hook+agent 묶음을 플러그인 1개로 패키징한다 — 5요소가 다 있어야 하는 건 아니다.)

## 외부 발행처 등록 표 (빈칸 ⬜ = 해당 발행 금지 → 로컬 파일로만 — SOUL §3)

커넥터(MCP)는 기본적으로 없다. 외부 연동이 필요해질 때 여기에 등록한 뒤에만 쓴다.

| 키 | 값 | 용도 |
|---|---|---|
| `SOURCE_REF` | ⬜ | 회의가 참조할 외부 자료 소스 (읽기 전용) |
| `REPORT_DEST` | ⬜ | 회의 레포트를 저장할 외부 위치 (scribe만 쓰기) |
| `LOG_DEST` | ⬜ | 발언 로그를 남길 외부 게시처 (미등록 시 `rounds/`로 충분) |
| `ANNOUNCE_DEST` | ⬜ | 레포트 링크를 알릴 외부 게시처 |

---

## 1. 절대 금지 — MCP 규약 (커넥터는 계약으로만 쓴다)

- ⛔ **등록 표가 비어 있는 대상에 MCP 쓰기를 호출하지 마라.** 읽기는 허용, 쓰기·게시는 빈칸이면 전면 금지.
- ⛔ **권한·범위를 정하지 않고 새 커넥터를 붙이지 마라.** 무엇을·왜·어떤 권한으로 읽고 쓰는지 먼저 이 표에 적는다.
- ⛔ **커스텀 MCP 서버를 새로 만들지 마라.** 기존에 등록된 커넥터로 안 되면 사람에게 보고만 한다.
- ⛔ **MCP로 읽어온 데이터를 검증 없이 EXTRACTED로 신뢰하지 마라.** 외부 출처 신뢰도는 INFERRED 이하 (SOUL §1).
- ⛔ **자료형 커넥터에서 "그냥 가져와" 식으로 요청하지 마라.** 가져오기 방식을 항상 명시한다 —
  **캡처(이미지)** 인지 **원본 데이터(예: 영역 전체 SVG)** 인지. (강의 예시: Figma MCP)
- ⛔ **외부 쓰기 도구를 scribe 외의 에이전트에 열어주지 마라.** 예외는 `LOG_DEST`의 자기 발언 게시뿐 (AGENTS §1).

## 2. 절대 금지 — 환경 (Windows · Node)

- ⛔ **훅을 bash로 작성하지 마라.** Windows에서 bash 훅은 깨진다. 훅은 **Node.js(.mjs)**로만.
- ⛔ **POSIX 전용 문법에 의존하지 마라.** `/dev/null`·`$VAR`·경로 구분 가정 금지 (PowerShell 환경).
- ⛔ **`node`가 없다고 가정한 채 진행하지 마라.** 훅은 node로 돈다. 없으면 멈추고 보고하라.
- ⛔ **프로젝트 폴더 이름에 한글·공백을 쓰지 마라.** 영문 이름으로. 경로 자체(드라이브·상위 폴더)는
  컴퓨터마다 달라도 된다 — 아래 이동 가능성 금지문 참조.

## 2-1. 절대 금지 — 이동 가능성 (컴퓨터가 바뀐다는 전제)

이 사용자는 여러 컴퓨터를 오가며 작업한다. **정본은 GitHub, 컴퓨터는 전부 일회용 체크아웃**이다.

- ⛔ **다른 하네스(원본 킷 등)를 하드코딩된 절대경로(`C:\work\...` 등)로 참조하지 마라.** 참조 대상은
  형제 디렉토리에서 이름·내용(README 첫 줄 등)으로 찾아 상대경로로 쓴다. 못 찾으면 추측하지 말고
  사람에게 경로를 물어라.
- ⛔ **git에 안 올라가는 파일에만 있는 상태를 "잃으면 안 되는 것"으로 두지 마라.** `state/`는 커밋된
  `log.md`·산출 폴더에서 재구성 가능해야 한다 — 재구성 절차 없는 로컬 전용 상태 금지.
- ⛔ **푸시 안 된 커밋을 남긴 채 세션을 닫지 마라.** 걸음마다 커밋, 세션 종료 전 반드시 `git push`.
  다음 세션은 다른 컴퓨터일 수 있다 — 푸시 안 된 커밋은 그 컴퓨터에 갇혀 사실상 유실이다.
- ⛔ **커밋되지 않은 걸음을 걸음 완료로 치지 마라.** 걸음의 원자 단위는 "산출·기록(state/log) +
  커밋"까지다 — 커맨드 절차의 "state 저장 + log append" 단계는 직후 커밋까지 마쳐야 끝난 것이다.
  크래시는 언제나 걸음과 커밋 사이를 노린다 — 세션 시작 시 git status로 미커밋 잔재를 먼저
  확인하고, 발견하면 새 걸음 전에 복구 커밋부터 한다. (v0.13 — 출처: academy-ops cycle 2 실패 1)
- ⛔ **커밋을 다른 git 명령과 &&로 묶지 마라.** 앞 명령(add 등)의 실패가 커밋 전체를 조용히
  생략한다 — add와 commit은 분리 실행하고, "커밋했다"는 직후 git log의 해시로만 증명한다 (SOUL §1).
  gitignore 대상 경로(state/ 등)를 add 인자에 넣지 마라 — 무시 경로는 커밋 세계에 존재하지 않는
  파일이고, 그 add 실패 하나가 확정 커밋을 통째로 삼킨 것이 실측된 실패 패턴이다.
  (v0.14 — 출처: academy-ops cycle 2 실패 3)
- ⛔ **state.cycle이 커밋된 최신 record/retro·log.md가 함의하는 사이클보다 작은 채로 다음 걸음을 시작하지
  마라.** 사이클 시작 커맨드(/meeting 등) 착수 전, 산출 폴더·log.md 마지막 `cycle N` 줄과 state.cycle을 대조해
  데싱크면 재구성한다 — gitignored 로컬 state는 컴퓨터마다 뒤처질 수 있고, 뒤처진 번호로 진행하면 기존 사이클
  산출을 덮는다. [승격 후보: session-start.mjs가 log.md 최신 cycle과 state.cycle 대조 → 불일치 시 재주입에 경고]
  (v0.34 — 출처: ai-worklog cycle 4, retro/2026-07-08-retro.md 실패 C)
  **[승격 이행됨: `.claude/hooks/session-start.mjs`의 `findUnloggedOutputs()` — 커밋된 `report/`(파생
  리포는 자체 `docs/` 등 확장) 산출 폴더가 log.md에 언급 없으면 다음 세션 시작 시 WARN, state.json
  유무와 무관하게 항상 실행.]** 이 벽은 state.cycle↔log.md 대조라 log.md를 아예 안 건드린 채 산출만
  커밋되는 사건은 못 잡았다(원 사건: academy-ops 2026-07-08, 교안이 log·state 무접촉으로 생산돼 4일간
  미결이 아무 경고 없이 방치됨) — 검출 없는 금지문은 불순종을 스스로 못 잡는다는 것이 재확인됐다.
  (v0.39 — 출처: academy-ops retro/2026-07-12-cycle3-retro.md 개정안 1, 사람 승인 2026-07-12)

- ⛔ **한 리포의 미커밋 산출물을 "버려진 잔재"로 판정해 다른 세션 대신 처리하지 마라 — 미커밋은 진행 중인 세션의 작업 파일일 수 있다.** cycle 8에 루트 세션이 활성 /posts 세션의 초안을 "미완·재개는 다음 세션 몫"으로 판정하고 구출 커밋했다(무해로 끝난 것은 운).
  [승격 후보: session-start가 `state/.session-lock`(session_id·ts·현재 걸음)을 쓰고 stop/retro-done에서 지운다 · 다른 세션의 커맨드 절차 0이 신선한(<N분) 락을 보면 경고. 승격 불가 부분: **다른 컴퓨터의 세션은 이 리포 로컬에서 관측 불가** — 진짜 조율은 오케스트레이터·사람 층이며, 락은 같은 체크아웃 안의 동시성만 잡는다]
  (v0.55 — 출처: ai-worklog cycle 8, retro/2026-07-19-retro-cycle8.md 실패 5 · 사용자 승인 2026-07-19.
  병행 세션 동시성은 모든 하네스에 있다 — 락의 관측 범위 한계(타 컴퓨터 비가시)를 벽에 함께 박제한다)

## 3. 절대 금지 — 권한 (에이전트별 스코프)

도구는 `.claude/agents/<name>.md`의 `tools:`로 제한된다. 이를 우회하지 마라.

- ⛔ **moderator·panelist·devil에게 외부 발행 도구를 기대하지 마라.** 읽기·자기 발언 기록까지만.
- ⛔ **scribe에게 코드 실행(Bash)을 시키지 마라.** 기록·합성·발행만.
- ⛔ **retrospector에게 산출물 수정 권한을 주지 마라.** Write는 `retro/`로만.
- ⛔ **메인 루프에서 평가를 직접 하지 마라.** 반드시 격리된 devil/retrospector에 위임.
- ⛔ **권한 완화 모드(`--dangerously-skip-permissions`)를 빈 전용 폴더 밖에서 쓰지 마라.** 부술 게 있는 곳에서 금지.
- ⛔ **커맨드가 배분하는 에이전트 타입이 레지스트리에 없다고 즉흥 대체하지 마라.** 폴백은 한 가지뿐이다 —
  general-purpose Worker에 해당 `.claude/agents/<name>.md`를 브리프 최상단에서 Read시켜 정체성·절대
  금지를 그대로 지우고, 그 파일의 `tools:` 제한과 산출물 경로를 브리프에 명시한다. 정체성 파일마저
  없으면 중단하고 사람에게 보고한다. 커맨드 진입 시 배분 대상 타입의 등록 여부를 먼저 확인하고,
  미등록 발견은 log.md에 한 줄 남긴다. (v0.12 — 출처: academy-ops cycle 1 실패 6)
- ⛔ **폴백 호출에서 `model:`을 빠뜨리지 마라.** 미등록 에이전트를 `general-purpose`로 대행할 때 복원해야 할
  것은 `tools:`(권한 상한)·산출 경로뿐이 아니다 — **`model:`(MODELS.md 배치)이 함께 복원되지 않으면 배치표는
  세션 기본 모델로 조용히 붕괴한다**(frontmatter가 MODELS.md의 유일한 집행 경로이고, 폴백에서는 그것을
  아무도 읽지 않는다). 폴백 호출은 Agent 도구의 model 파라미터에 그 에이전트의 역할군 값을 **명시**하고,
  그 사실을 log.md에 남긴다.
  [승격 후보: 폴백 절차를 **체크리스트로 박제** — ① frontmatter Read ② `tools:` 복원 ③ **`model:` 복원**
  ④ 산출 경로 ⑤ 죽은 훅 목록 선언(§4 v0.40의 승격 후보 ③과 한 걸음). 각 항목의 이행을 log.md 한 줄에 기록]
  (v0.41 — 출처: ai-worklog cycle 5, retro/2026-07-12-retro-cycle5.md 실패 6)
- ⛔ **에이전트 레지스트리의 등록 상태를 리포의 속성으로 취급하지 마라 — 세션의 속성이다.** 같은
  리포에서도 세션마다 등록 목록이 다른 것이 실측됐다(cycle 2: 시작 세션 미등록·폴백, 확정 세션
  정식 등록). 이전 세션의 log.md 등록/미등록 기록을 이번 세션의 배분 근거로 재사용하지 마라 —
  커맨드 진입 시 등록 확인은 세션마다 새로 한다. (v0.15 — 출처: academy-ops cycle 2 관찰 1)

## 4. 절대 금지 — 훅 배선

- ⛔ **훅에서 절대 throw 하지 마라.** 크래시 = 통과 = 누수다. 훅은 조용히 차단하거나 조용히 통과한다.
- ⛔ **pre-tool-use 가드가 있다고 소스 계층 덮어쓰기를 시도하지 마라.** 막히기 전에 시도조차 하지 마라.
- ⛔ **stop 훅의 회고 가드를 우회해 세션을 끝내지 마라.** 회고 파일이 없으면 stop은 차단된다 (SOUL §6).
- ⛔ **훅이 외부 발행을 직접 실행하게 만들지 마라.** 훅은 감지·차단·되넘김만. 실행은 인세션 에이전트가 한다.
- ⛔ **하네스 커맨드(/meeting·/meeting-round·/retro)를 동명 Skill·플러그인 호출로 대체하지 마라.** 절차의 원본은 `.claude/commands/*.md`다. 로드된 절차의 출처 경로가 이 프로젝트가 아니면 즉시 중단하고 해당 커맨드 파일을 직접 Read로 읽어 따른다.
- ⛔ **커맨드 실행 시 로드된 절차의 출처를 확인하지 않은 채 진행하지 마라.** `/<커맨드>` 실행
  첫 동작은 `.claude/commands/<커맨드>.md`를 Read해 그 파일의 절차를 따르고 있음을 확인하는 것이다.
  외부 Skill(`~/.claude/skills/<동명>`)이 먼저 로드됐다면 그 절차를 버리고 리포 원본으로 재시작한다
  (2026-07-12 cycle 1 실측: `/retro`가 외부 동명 스킬로 로드됨 — 기억에 의존한 복구는 다음번에
  실패한다). [승격 후보: session-start.mjs 재주입 배너에 ~/.claude/skills ↔ .claude/commands 이름
  충돌 경고 추가 — 교집합이 비어있지 않으면 "⚠ 커맨드 이름 충돌: <name>" 강제 재주입. 홈 접근 실패는
  try/catch로 조용히 통과(훅 throw 금지 규약)] (v0.38 — 출처: somnia-hub cycle 1, retro/2026-07-12-retro.md 실패 4)
- ⛔ **커맨드 md에 셸 명령 블록을 박제하기 전에, 그 명령을 project-walls(engine 경유)에 실제로
  흘려 deny 여부를 실측하지 않은 채 배포하지 마라.** 벽 매트릭스는 벽만 검증한다 — 커맨드에 박제된
  명령이 그 벽에 걸리면 사이클은 막다른 골목에서 즉흥 우회(세션 임시 경로 등)를 만들어낸다
  (2026-07-12 cycle 1: `/fleet-scan` 절차 2 루프 + `Out-File` → H3c deny → 원자료 휘발 →
  스캔 자립 규약 파손). 커맨드가 처방한 명령은 우리 벽을 통과해야 한다 — 통과 못 하면 명령을 고친다
  (벽이 아니라). [승격 후보: 벽 매트릭스 하네스에 케이스군 추가 — .claude/commands/*.md의 셸
  코드블록을 전수 추출해 engine 경유로 흘리고, deny가 나오면 매트릭스 FAIL. 케이스 목록은 각
  프로젝트가 채운다] (v0.38 — 출처: somnia-hub cycle 1, retro/2026-07-12-retro.md 실패 5)
- ⛔ **파괴 패턴 검사를 명령 문자열 전체에 평면 매칭하지 마라.** 검사는 실행 구조(연산자·cmdlet 호출
  위치)에 앵커하고, 개행을 문장 경계로 취급한다(패턴은 `[^|;&\n]`류로 줄 넘김을 차단). 따옴표 안
  데이터(인용문·서열 문구·테스트 페이로드)의 오탐은 알려진 한계로 훅 주석에 명시한다. 오탐으로
  차단되면 표준 우회는 한 가지뿐이다 — 내용을 임시 파일에 두고 append로 전달. 훅 무력화·매처
  해제로 우회하지 마라. (v0.10 — 출처: academy-ops cycle 1 실패 4)
- ⛔ **훅 매처를 Bash에만 걸지 마라.** 환경에 존재하는 모든 셸 실행 도구(PowerShell 등)를 같은 검사에
  태운다. POSIX 명령 이름만 검사하지 마라 — 동등 cmdlet(Remove-Item·Clear-Content·Set-Content·
  Out-File·Move-Item 등) 패턴과 셸별 예외(`-Append` 등)를 대칭으로 둔다. 셸 도구·패턴을 바꿨으면
  deny·통과 양쪽 케이스를 재검증하기 전까지 벽이 있다고 말하지 마라 (SOUL §1).
  (v0.11 — 출처: academy-ops cycle 1 실패 5, 훅 코드·매처 동반 반영)
- ⛔ **`engine.mjs`(벽2 공용 로직)를 프로젝트별로 고치지 마라.** 프로젝트 차이는 `harness.config.json`(상수)와
  `project-walls.mjs`(고유 벽)로만 낸다 — engine 은 전 함대 바이트 동일이어야 공통 벽 개정이 복사 1회로
  전파되고, F1(PowerShell 미전파) 같은 형제 누락이 재발하지 않는다(해시 감사 대상). 상수로 표현 안 되는
  프로젝트 고유 로직은 project-walls 로 분리한다. (v0.16 — 출처: HANDOFF W3, 훅 3층 분리)
- ⛔ **이전 커맨드의 걸음이 파일로 완결 확인되기 전에 다음 커맨드를 시작하지 마라.** 새 커맨드가 수신되면
  먼저 이전 걸음을 닫고(산출물 Read 재검증 + state 저장), 그 다음 실행한다 — 두 걸음 병렬 진행 금지
  (SOUL §4 "한 걸음 = 한 단계"의 커맨드 경계 확장). [승격 후보: state.json에 미결 하위상태 필드(pending) +
  각 커맨드 절차 1 게이트가 status와 pending을 함께 검사] (v0.23 — 출처: ai-worklog cycle 2, retro/2026-07-06-retro.md 실패 3)
- ⛔ report_done 상태에서 커맨드가 '다음 걸음 /retro'를 **안내만 하고 턴을 종료하지 마라** — 회고는 사용자 승인 없이 이어져야 하는 강제 걸음이다(stop-retro-guard가 차단하는 이유). 종료 보고는 '회고로 이어감'을 명시하고 같은 흐름에서 /retro를 착수하거나, 명시적 사용자 대기가 필요하면 그 이유를 한 줄로 밝힌다. 안내 문구가 무언의 대기 신호가 되게 두지 마라. [승격 후보: stop-retro-guard 차단 메시지에 '안내 종료 금지' 문구 연결(반)] (v0.26 — 출처: invest-desk cycle 1, retro/2026-07-06-retro.md 실패 4)
- ⛔ slop 패턴을 대소문자 무시 단어경계 정규식만으로 두지 마라 — 고유명사·인용 개체명(선행 토큰이 대문자, 또는 알려진 회사/제품명)을 warn 대상에서 제외하는 예외를 붙인다. 최소 조치로 slop 어휘가 대문자로 시작해 고유명사 위치에 있으면 warn하지 않는다. 오탐 반복은 경고 무감각화를 부르며, 이는 slop 게이트의 존재 목적을 무력화한다. [승격 후보: post-tool-use.mjs 패턴 개정 — 완전 기계화] (v0.27 — 출처: invest-desk cycle 1, retro/2026-07-06-retro.md 실패 5)
- ⛔ **훅이 로드됐는지 확인하지 않은 채 하네스 커맨드를 실행하지 마라.** 프로젝트 에이전트가 레지스트리
  미등록이면(v0.12 폴백 조건) 그것은 **이 세션의 프로젝트 루트가 이 리포가 아니라는 뜻**이고, 그때
  `.claude/settings.json`의 훅 4종은 **전부 죽어 있다** — 벽2·벽5·slop·사건 장부·회고 가드가 없는 세션이다.
  폴백으로 진행하기로 했으면 착수 전에 (a) 죽은 벽 목록을 명시하고 (b) 훅이 대신하던 기계 검사
  (`spec-check`·`step-diff`)를 **수동으로 반드시** 돌리고 (c) 그 사실을 `log.md`와 회고에 기재한다.
  [승격 후보 ① `ledger.mjs` heartbeat: `session-start`가 장부에 세션 개시 1줄(`{"kind":"session-start", ...}`)을
  남긴다 — 그러면 **"사고 0"(heartbeat 있음 · 사건 0)과 "훅 미작동"(파일 완전히 빔)이 기계적으로 구별된다.**
  현재는 두 상태가 **같은 빈 파일**이다. (⚠ `ledger.mjs`는 전 함대 바이트 동일·해시 감사 대상 — 이 승격은
  함대 전파를 동반해야 한다) **[① 이행됨: ledger.mjs v2 heartbeat — 전 함대 해시 단일]** ② `checks/hooks-alive.mjs` 신설: `CLAUDE_PROJECT_DIR`(또는 `settings.json`
  로드 흔적)를 이 리포와 대조해 훅 생존을 판정 — 각 커맨드 절차 1이 이것을 먼저 부르고, 미로드면 죽은 벽
  목록을 출력한다. ③ v0.12 폴백 규약 본문에 ①②를 절차로 박제 (폴백 = 에이전트 복구 + **훅 부재 선언** 두 걸음)]
  (v0.40 — 출처: ai-worklog cycle 5, retro/2026-07-12-retro-cycle5.md 실패 1)
- ⛔ **빈 사건 장부를 "무사고"로 읽지 마라.** 장부가 비었으면 먼저 **"훅이 돌았는가"**를 확인한다.
  훅이 안 돈 세션의 빈 장부는 무사고의 증거가 아니라 **관측 부재의 증거**다 (SOUL §1 — 부재는 증명한 뒤에만).
  (v0.40 — 출처: ai-worklog cycle 5, retro/2026-07-12-retro-cycle5.md 실패 1)
- ⛔ **한 검사 스크립트 안에서 같은 개념(문장·문단·글자)에 서로 다른 정의를 쓰지 마라.** `spec-check.mjs`의
  `sentences()`(201행)와 `sentenceCount()`(202행)가 **문장 경계를 다르게 정의**해, 마침표 뒤 닫는 따옴표로 끝나는
  문장에서 **거짓 100자 초과 WARN**을 냈다. 개념 정의는 파일 안에서 **1벌**이어야 한다.
- ⛔ **검출기의 오탐에 산출물을 맞추지 마라 — 오탐은 고치는 것이지 순응하는 것이 아니다.** cycle 6 cafe는
  거짓 WARN을 없애려 구두점을 옮겼고 산문은 오히려 길어졌다(2710 → 2906자, WARN 2 → 0). **검출기가 산문이
  아니라 산출물의 형태를 바꾼 것**이며, 이것은 경고 무감각화(CLAUDE §4 slop 벽)보다 **한 단계 나쁜 피해**다.
  WARN이 산출물의 형태 변경으로만 해소된다면 **WARN을 의심하고 소스를 읽어라.**
  **⚠ 킷 역이식 범위 (2026-07-12 실측 정정)**: 출처 회고는 `spec-check.mjs`를 "전 함대 공통 파일"이라 적었으나
  **실측 결과 그 파일은 ai-worklog에만 존재한다**(킷에도, 다른 어느 리포에도 없다 — 함대 전수 검색 EXTRACTED).
  따라서 킷이 수신하는 것은 **위 두 산문 벽뿐**이다 — 모든 하네스의 **검사 스크립트 일반**에 유효한 원칙으로
  받는다(같은 개념 1벌 정의 · 오탐에 순응 금지). **코드 개정(`sentences()` 정규식 + 회귀 3케이스)은
  ai-worklog 단독**이며, 킷에는 대응 파일이 없어 함대 해시 재단일 대상이 아니다.
  이 정정 자체가 이 벽의 실례다 — **회고가 "공통 파일"이라 단언한 것을 실측이 반증했다** (SOUL §1: 부재는
  증명한 뒤에만 · 존재도 마찬가지다).
  [승격 후보(ai-worklog 단독): ① `sentences()`의 분할기를 `/(?<=[.!?…][”"'\)\]]?)\s+/`로 고쳐 202행과 같은
  문장 규칙을 쓰게 한다 ② 회귀 케이스 3건 박제(인용 종결·괄호 종결·평문) — 두 함수가 같은 문장 수를 내는지 검사.
  킷 공통 승격 후보: 새 검사 스크립트를 만들 때 "같은 개념의 정의가 파일 안에 2벌 이상인가"를 코드 리뷰 항목으로]
  (v0.44 — 출처: ai-worklog cycle 6, retro/2026-07-12-retro-cycle6.md 실패 3 + 말미 "실측 정정" 절)
- ⛔ **장부에 heartbeat가 있다는 이유로 "훅이 돌았다"고 읽지 마라.** 판독 조건은 "heartbeat 있음"이 아니라
  **"이번 사이클(`state.cycle`)의 heartbeat가 있음"**이다. cycle 7의 장부는 heartbeat 1줄 + 그 외 0줄이었고 —
  `ledger.mjs`:21의 판독 규칙을 문면대로 적용하면 **"(A) 무사고 · 훅이 돌았다"**가 나오는데, **훅은 이 사이클
  내내 죽어 있었다.** 그 1줄은 **cycle 6**의 것이었다(ts 17:07:46 · cycle 7의 첫 커밋은 17:42:10).
  **빈 장부의 모호성은 사라진 게 아니라 낡은 heartbeat의 모호성으로 자리를 옮겼다.**
- ⛔ **회고를 닫으면서 사건 장부를 비우지 않은 채 다음 사이클을 열지 마라** (`/retro` 절차 6). 비워지지 않은
  장부는 **직전 사이클의 heartbeat를 이번 사이클의 생존 증거로 위조한다.**
- ⛔ **훅의 수동 검증 실행이 장부에 진짜 세션과 구별 불가능한 줄을 남기게 하지 마라.** 검증 실행은
  `{"kind":"session-start","verify":true}`처럼 **자기를 표시**해야 한다 — 관측기를 시험하는 행위가 관측 기록을
  오염시키면, 그 관측기는 자기 시험을 관측할 수 없다.
  **이것은 v0.40 승격 후보 ①(`ledger heartbeat`)의 첫 실전 판정이다** — heartbeat는 "빈 장부 = 무사고 / 훅 미작동"의
  모호성을 **없앤 게 아니라 자리를 옮겼다**: 이제 둘은 **같은 1줄 파일**이고, 구별하는 것은 그 줄의 `cycle` 필드뿐이다.
  ⚠ `ledger.mjs`는 전 함대 바이트 동일·해시 감사 대상 — 이 판독 규칙(주석)의 개정은 **함대 전파를 동반해야 한다**.
  [승격 후보 → **이행됨 2026-07-19**: `.claude/checks/hooks-alive.mjs` 킷 배치(원본 ai-worklog 역이식·범용화) +
  sam·invest-desk·brand-radar·somnia-hub 동시 전파(해시 동일 5벌 · LoL은 v0.8 구조라 대상 외). 아래 ①③④ 포함
  구현 — 잔여: ② "각 커맨드 절차 0이 호출" 배선은 커맨드 md 개정이라 벽 매트릭스 실측과 함께 별도 걸음.]
  [원문: `checks/hooks-alive.mjs`(§4 v0.40 승격 후보 ②)를 **가장 싼 형태로** — ① 장부의 마지막 heartbeat의
  `cycle`을 `state.cycle`과 대조 → 불일치·부재면 "훅 미작동" 출력 ② 각 커맨드 절차 0이 이것을 호출 ③ 같은
  스크립트가 `/retro` 절차 6의 **장부 비우기 이행**도 검사(비어 있지 않은 채 `retro_done` 전이 시 경고)
  ④ `appendHeartbeat`에 `verify` 플래그. **승격이 다음 승격의 입력을 만든 첫 사례다** — 재료는 이미 장부에 있다]
  (v0.47 — 출처: ai-worklog cycle 7, retro/2026-07-12-retro-cycle7.md 실패 1 · `/retro` 절차 1-1·6과 한 벌)
- ⛔ **검사기가 WARN을 내면서 그 WARN의 위치를 함께 찍지 않게 두지 마라.** **위치 없는 WARN은 읽는 쪽이
  위치를 지어내게 만든다** — 이번엔 critic이 지어냈다. **위치를 아는 것은 검사기다.**
  (모든 하네스의 검사 스크립트에 유효한 원칙으로 받는다 — v0.44 《검출기의 오탐에 산출물을 맞추지 마라》의
  직접 보완이다: v0.44는 **읽는 쪽**에게 "WARN이 뜨면 소스를 읽어라"를 시켰고, 이 벽은 **검사기 쪽**에게
  "읽을 필요가 없게 위치를 찍어라"를 시킨다. 실측(ai-worklog cycle 7): 어미 3연속 WARN에 위치가 없어 critic이
  **포스트 1 내부**로 지목했으나 진짜 런은 **포스트 경계**였다 — 검사기는 런의 위치를 이미 알고 있었고
  출력하지 않았을 뿐이다.)
  [승격 후보(각 하네스 단독): 검사 스크립트의 **모든 WARN에 문장 인덱스 + 발췌 30자를 병기**. 킷에는 대응
  검사 파일이 없어 함대 해시 재단일 대상이 아니다 — 킷이 수신하는 것은 **위 산문 벽뿐**이다]
  (v0.50 — 출처: ai-worklog cycle 7, retro/2026-07-12-retro-cycle7.md 실패 4 · 혼합 범위의 원칙 부분)

- ⛔ **하네스 벽에 "사용자 지시로도 우회되지 않는다"고 적지 마라 — 어떤 벽도 서열 1(사용자)을 이기지 못한다(SOUL 서열).** 벽이 할 수 있는 최대치는
  **침묵 우회를 막고 사용자의 명시적·기록된 개방을 강제**하는 것이지, 사용자를 차단하는 것이 아니다. `defer-gate.mjs`:101의 절대 문구는 cycle 5·8 두 번 사용자에게 우회됐고 —
  **선언한 절대성이 두 번 다 거짓으로 판명됐으며 코드는 지금도 exit 1로 남아 행동과 어긋난다.**
  [승격 후보: defer-gate에 `--user-override <사유>` 경로 — 통과시키되 exit 0 대신 **exit 3(개방 기록됨)**을 내고 장부에 `{kind:"gate-override",by:"user",reason}` 1줄을 남긴다. 우회는 침묵이 아니라 **기록된 사건**이 된다. 무플래그 실행은 그대로 exit 1(침묵 우회 차단 유지)]
  (v0.53 — 출처: ai-worklog cycle 8, retro/2026-07-19-retro-cycle8.md 실패 3 · 사용자 승인 2026-07-19.
  게이트가 서열 1과 충돌하는 구조는 모든 하네스에 있다 — 벽의 절대 선언은 두 번 다 거짓이 됐고, "명시 개방의 기록"만이 벽이 할 수 있는 전부다)
- ⛔ **벽을 사용자 지시로 통과시킨 채 코드를 안 고치고 사이클을 닫지 마라 — "우회했다"는 "코드가 그 상태를 반영한다"가 아니다.** 통과가 코드까지 바꾼 것인지 걸음만 지나간 것인지 회고에 구분해 적고, 어긋난 코드는 개정안으로 올린다(record §5).
  [승격 불가: 코드-행동 일치 여부는 사람이 판단 — 단 위 override 경로가 이행되면 "우회 = 기록"이 되어 괴리 자체가 사라진다]
  (v0.53 — 출처: ai-worklog cycle 8, retro/2026-07-19-retro-cycle8.md 실패 3)

## 5. 절대 금지 — 상태 · 루프

- ⛔ **`state/`를 저장하지 않고 걸음을 끝내지 마라.** 세션이 죽어도 다음 세션이 파일에서 이어받아야 한다.
- ⛔ **레포트 발행 전에 `STATE: done`을 출력하지 마라.** done 조건 = 레포트 발행 완료 + 회고 작성 완료.
- ⛔ **한 커맨드 호출에서 여러 라운드를 몰아 돌리지 마라.** `/meeting-round`는 라운드 1회만. 루프는 `/loop`가 돈다.

## 6. CronJob 규약 (예약 작업)

- ⛔ **크론 세션이라고 서열을 건너뛰지 마라.** 무인 세션도 SOUL > AGENTS > CLAUDE 그대로다.
- ⛔ **크론에서 새 안건을 창작하지 마라.** 크론은 정해진 안건 소스(로컬 안건 파일 또는 `SOURCE_REF`)에서만 가져온다.
- 예약 예시: 주간 회의 = 매주 월 10:00 `/meeting`(안건: 안건 소스의 미처리 항목) · 주간 회고 = 매주 금 17:00 `/retro`.

---

## 7. 배선 사실 (참고 — DO)

- 훅 구조(3층 · HANDOFF W3): `.claude/hooks/engine.mjs`(벽2 공용 로직 · 전 함대 바이트 동일 · 프로젝트별 수정 금지) +
  `harness.config.json`(프로젝트 상수 protected/immutableDirs/appendableDirs/mutableDirs) + `ledger.mjs`(사건 장부) +
  진입점 5(session-start·pre-tool-use·post-tool-use·stop-retro-guard·agent-ledger). 고유 벽은 `project-walls.mjs`(선택 · 킷엔 없음).
- 훅 연결: `.claude/settings.json` — SessionStart=startup/resume/clear/compact(재주입: SOUL+TOP-WALLS+상태) ·
  PreToolUse=`Write|Edit|MultiEdit|Bash|PowerShell`(engine: append-only + rounds/ 불변) · PostToolUse(slop·증거부실 경고 +장부) ·
  PostToolUse=`Agent|Task`(agent-ledger: 서브에이전트 호출 장부 → `state/agent-calls.jsonl` 로컬 전용 — 캘리브레이션
  provenance의 기계 증거, 매니페스트 생성·대조는 model-gym/README "provenance 매니페스트" 절) ·
  Stop(회고 가드 — `status=report_done`인데 회고 없으면 차단, attempts 3회 상한 후 포기·통과 시 `log.md`·장부 기록).
- 걸음 게이트: `.claude/checks/step-diff.mjs`(HANDOFF W5) — 커맨드가 에이전트 배분 직후 실행, 선언 산출 경로 밖 변경이면 exit 1(REVISE).
- 훅은 절대 throw 하지 않는다(크래시=통과=누수). 작동 점검: `node .claude/hooks/session-start.mjs` → 재주입 JSON.
- 상태 파일: `state/state.json`(cycle·round·status·votes·report_path·retro_path) · `state/incidents.jsonl`(사건 장부 — 로컬 전용/gitignore) — 매 걸음 저장.
- 산출 구조: `rounds/`(발언 원본, 불변) · `wiki/`(정리) · `report/`(레포트) · `retro/`(회고) · `log.md`(한 줄 기록).
- 모델 배선(HANDOFF W7): `MODELS.md`(역할군별 모델 단일 원천) — 에이전트 frontmatter `model:`은 이 표를 따른다, 모델 참조 하드코딩 금지.
- 에이전트 정의: `.claude/agents/*.md` — Role·페르소나·tools 제한 포함.
- 역이식: `/retro`가 승인된 개정안을 이 프로젝트 + 원본 킷(형제 디렉토리 중 README 첫 줄이 `# harness-kit`인 폴더) + 양쪽 `CHANGELOG.md`에 동시 반영.
